echo "Welcome to the MySQL monitor."

select op in "Create Database" "List Database" "Connect to Database" "Drop Database"
do
    case $op in
        "Create Database" )
        ;;
        "List Database" )
        ;;
        "Connect to Database" )
        ;;
        "Drop Database" )
        ;;
    esac
done

